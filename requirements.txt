# Minimal RunPod dependencies only
# (FLUX requirements will be installed by Dockerfile from local diffusers folder)
requests>=2.31.0
Pillow>=10.0.1

# Core Python dependencies
packaging>=21.3
setuptools>=68.2.2
wheel>=0.41.2
